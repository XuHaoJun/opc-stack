# Secrets / local config
.env
.env.bak
*.local.env

# Logs / scratch
*.log
/tmp/

# Upgrade volume backups (created by the upgrade-opc-stack skill)
backups/

# Editor/OS noise
.DS_Store
*.swp

# Python (hermes memory plugins in patches/)
__pycache__/
*.py[cod]

# Node deps (acp-smoke-test.mjs runs inside the paperclip container, not here)
node_modules/
